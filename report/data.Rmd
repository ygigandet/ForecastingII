## Data

```{r, echo = FALSE, message = FALSE, warning=FALSE}
source(here::here("scripts/setup.R"))
```

```{r}
# Download the dataset
setwd(here::here("data/"))
bikesharing <- load(here::here("data/Bikesharing_dataset.Rdata"))
```

```{r}
# Creation of time series
Bietschhorn <- as_tsibble(series$Bietschhorn)
Blancherie <- as_tsibble(series$Blancherie)
Cible <- as_tsibble(series$Cible)
GSD <- as_tsibble(series$`Gare de Sion Sud`)
PRP <- as_tsibble(series$`P+R Potences`)
Platta <- as_tsibble(series$Platta)
Planta <- as_tsibble(series$Planta)
CRR <- as_tsibble(series$`Hôpital - CRR`)
Campus <- as_tsibble(series$`Campus Energypolis`)
Scex <- as_tsibble(series$Scex)
Furet <- as_tsibble(series$`Châteauneuf-Furet`)
Agasse <- as_tsibble(series$Agasse)
Stade <- as_tsibble(series$`P+R Stade`)
GSN <- as_tsibble(series$`Gare de Sion Nord`)
Stand <- as_tsibble(series$`Ancien Stand`)
Bramois <- as_tsibble(series$`UNIL/UNIGE Bramois`)
```
